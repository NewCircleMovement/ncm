= render layout: "epicenters/edit_epicenter" do
  
  - if @epicenter.fruittype.present?
    .alert.alert-success
      %i.fa.fa-check
      Epicenteret har en frugttype
    .row
      .col-md-4
        .well.well-sm
          %strong= @epicenter.fruittype.name
          = link_to "Ret", edit_epicenter_fruittype_path(@epicenter, @epicenter.fruittype)
  - else
    .alert.alert-danger
      %i.fa.fa-times
      Der er ikke oprettet nogen frugttype
    = link_to "Opret frugttype", new_epicenter_fruittype_path(@epicenter), class: "btn btn-primary"
    