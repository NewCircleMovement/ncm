= render layout: "epicenters/edit_epicenter" do

  - if @epicenter.location.access_points.present?
    .alert.alert-success
      %i.fa.fa-check
      Epicenteret har roller
    .row
      - for access in @epicenter.location.access_points
        .col-md-4
          .well.well-sm
            %strong= access.name
            = link_to "Ret", edit_epicenter_access_point_path(@epicenter, access)
  - else
    .alert.alert-danger
      %i.fa.fa-times
      Der er ikke oprettet nogen frugttype
  
  = link_to "Opret ny rolle", new_epicenter_access_point_path(@epicenter), class: "btn btn-primary"
    