= simple_form_for @epicenter do |f|
  = f.input :name
  = f.input :description
  = f.input :video_url
  = f.input :depth_members
  = f.input :depth_fruits
  = f.input :monthly_fruits_basis
  
  = f.submit "Gem", :class => 'btn btn-primary'
  = link_to "Fortryd", :back, class: "btn btn-default"
